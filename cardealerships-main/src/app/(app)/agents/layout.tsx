export default function AgentsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // TODO: Sidebar for agents pages
  return (
    <div>
      {/* Agents sidebar navigation */}
      {children}
    </div>
  );
}
