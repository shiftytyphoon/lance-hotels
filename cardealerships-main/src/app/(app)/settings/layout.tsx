export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // TODO: Settings sidebar navigation
  return (
    <div>
      {/* Settings sidebar */}
      {children}
    </div>
  );
}
